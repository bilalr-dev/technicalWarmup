/*
1-Array : 
A data structure that can store 
many values of the same data type,
it is fixed size data structure,
meaning the number of elements 
cannot change once defined.
    Example: 
        double prices[]= {5.5, 6.5, 7.5, 8.5};
        char name[]= "Bilal";
        int numbers[5];
Arrays use index to access elements, 
the first element is at index 0.
*/