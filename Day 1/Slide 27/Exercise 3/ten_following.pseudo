"""
Write an algorithm that asks for an 
integer an display the 10 following numbers
"""
Begin
    Variable inputNumber, counter as Integer
    Write "Enter a number : "
    Read inputNumber
    Write "The next 10 numbers are : \n"
    For counter <- 1 to 10 do 
        Write inputNumber + counter + "\n"
End